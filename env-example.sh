#!/bin/bash

# Generate a personal access token (PAT) on GitHub and set it here.
# Go to $GITHUB_HOST/settings/tokens and create a new token with the appropriate scopes.

export GITHUB_PERSONAL_ACCESS_TOKEN=YOUR_PAT
export GITHUB_HOST=https://git.standard.re
export REF_API_KEY=YOUR_REF_API
export BRAVE_API_KEY=YOUR_BRAVE_API_KEY
